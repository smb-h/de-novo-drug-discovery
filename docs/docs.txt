# save model, model weights, model structure+
check scatter plot (with genetic algorithm)
plot (scatters plot) smiles (PCA)

RDKIT -> validity, uniqueness, originality on generated smiles -> output report

add new models
train each model and compare results


Add test data to data loader 90 10 10
add predict and visualization
