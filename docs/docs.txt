check model results
plot drug (smiles)
export model params to config.js in experiment
pros and cons (input output types)
evaluation
lstm
    plot data density (genetic algorithm, pca)
check scatter plot (with genetic algorithm)

####
check one hot encoding of two paper

clean code
plot loss and accuracy

# save model, model weights, model structure+

plot (scatters plot) smiles 
